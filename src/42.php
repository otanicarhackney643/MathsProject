<?php
// Define variables and constants from math project
$a = 5;
$b = 3;
$c = 2;
$d = 4;

// Add variable names to the array
Array_to_string($variableNames) {
    return $variableNames;
}

// Check if variables are defined
if (isset($a, $b, $c, $d)) {
    // Code here...
}
